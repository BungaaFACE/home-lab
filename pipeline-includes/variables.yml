variables:
  DESKTOP_PATH: "/home/bungaaface/Desktop"
  COMPOSE_PATH: "${DESKTOP_PATH}/docker-composes"
  DATA_PATH: "${DESKTOP_PATH}/container-data"
  SITES_PATH: "${DESKTOP_PATH}/sites"
  STATIC_PATH: "${DESKTOP_PATH}/static"
  SSL_PATH: "${DESKTOP_PATH}/ssl"